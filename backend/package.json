{
  "name": "smail-backend",
  "version": "2.0.0",
  "description": "SMail backend — run from project root with: npm start",
  "main": "server.js",
  "private": true,
  "note": "Dependencies are managed by the root package.json. Run 'npm install' and 'npm start' from the project root, not this directory.",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.3.1",
    "express-session": "^1.19.0",
    "google-auth-library": "^10.6.2",
    "googleapis": "^171.4.0",
    "jsonwebtoken": "^9.0.3",
    "sqlite3": "^6.0.1"
  }
}
